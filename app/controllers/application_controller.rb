class ApplicationController < ActionController::API
end
# stream_from "some_channel"
    #     puts "new subscriber on messages_channel"
    #  conversation = Conversation.find(params[:conversation])
    #  puts "streaming on #{conversation.id}"

    # #  return the messages from an individual conversation
    #     stream_from "conversation_#{conversation.id}"




    # def subscribed
  #   # stream_from "some_channel"
  #   puts "we have a subscriber on conversations channel"
  #   # the data gets sent straight too the subscriber on the client side ofthe conversations_channel
  #   stream_from "conversations_channel"
  # end

  # def unsubscribed
  #   # Any cleanup needed when channel is unsubscribed
  # end




